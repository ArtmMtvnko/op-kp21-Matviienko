using System;
using System.Security.Cryptography.X509Certificates;

class Pyramid
{
    static void Main(string[] args)
    {
        Console.WriteLine("Enter a Sinus value (in degrees): ");
        int degrees = Convert.ToInt32(Console.ReadLine());
        double x = degrees * (Math.PI / 180);
        /*
         * input value (in degrees) -> convert to radians
         * 
         * write function for factorial 
         * 
         * calculate according formula
         * 
         * output the result
         */
        Console.WriteLine("Value is: ");
    }

}